# 이미지 교체(배포용)
#
# 파일명 규칙:
#   {항목ID}-{순번}.png  또는  .jpg
# 예:
#   engine-01-1.png
#   chassis-03-2.jpg
#
# 화면의 [이미지 교체]를 누르면 같은 이름 파일이 다운로드됩니다.
# 이 폴더에 넣은 뒤 auto.bat 하면 전체에 반영됩니다.
#
# 우선순위: 로컬교체(IndexedDB) > 이 폴더 > default/
